# GLM20 Handoff - 2026-05-17

This paper is paused mid-Section 5. The next agent should read
`START_HERE_NEXT_AGENT.md` first for the short startup path, then use this file
for the longer proof-state ledger.

## Validation

The latest targeted proof check passed before this handoff:

```bash
lake build GLM20DroppingStandardizedTesting.PaperInterface
```

The worktree is shared. Stage only explicit GLM20 paths and skill files you
intend to own; do not use broad `git add .`.

## Current Closed Surface

Lemma 1, Proposition 1, Lemma 2, Theorem 1, Theorem 2, Lemma 3, and large parts
of Propositions 2, 3, 5, 6, and Theorem 3 have paper-facing endpoints in
`PaperInterface.lean`. The first-read status is in `README.md`; the important
current caveats are:

- Theorem 1 is proved on the explicit low-capacity/interior-selectivity domain
  `2 * capacity < pi * gammaB` and
  `2 * capacity < (1 - pi) * gammaA`.
- Theorem 2's main-text high-skill direction is proved under the extra
  test-free precision order `subB < subA`. The appendix route proves the
  opposite high-skill direction under `subA < subB`; keep this as a source
  statement/proof caveat.
- Proposition 3's displayed `lambda` formula differs from the verified
  first-moment/Owen expression by a boundary term except in degenerate cases.
- Section 5 is not fully closed: the remaining work is concrete strategic
  applicant-pool/objective instantiation, not another abstract restatement.

## Recent Section 5 Additions

Theorem 3 now has concrete feasibility-aware endpoints beyond the older
source-shaped condition wrappers:

- `paper_interface_theorem3_standardGaussian_source_conditions_of_policy_state_table_j2_zero_fallback_subFull_interval_of_equation50`
- `paper_interface_theorem3_policyStateSubFullMassFeasibleSurface_subFull_fullSub_interval`
- `paper_interface_theorem3_policyStateSubFullMassFeasibleSurface_fullFull_condition_of_positive_cost_interval`
- `paper_interface_theorem3_policyStateSubFullMassFeasibleSurface_source_conditions_of_i_ii_and_positive_cost_interval`

These compose the condition-(11) feasible surface, equation-(50) regularity,
sub/full and full/sub interval endpoints, and positive-cost full/full endpoint.
They are useful because the next proof should feed concrete Gaussian model
premises into these wrappers rather than duplicate Theorem 3's large
condition signatures.

Proposition 6 now has a cleaner generated-row endpoint:

- `paper_interface_proposition6_policyState_source_parameter_diversity_rows_inequality_implies_subFull_equilibrium_of_J2_fallback`
- `paper_interface_proposition6_policyState_standardGaussian_source_parameter_diversity_rows_inequality_implies_subFull_equilibrium_of_J2_fallback`

The point is small but useful: the generated Proposition 6 diversity row only
overrides school `J1`, so under `J1 != J2` the `J2` weak no-deviation premise
can be stated on the ordinary fallback row. This avoids carrying generated-row
clutter in future source-model wiring.

## Best Next Proof Seam

Stay in Section 5 and work result by result. Do not move to later appendix
generalizations until Theorem 3 / Proposition 6 strategic instantiation is
genuinely closed or a mathematical blocker is found.

1. Prove concrete objective/mass/no-tie identities for the two-school Gaussian
   game and feed them into the strongest Theorem 3 wrappers above.
2. Use the existing policy-state table and fixed-pool admitted-merit endpoints;
   do not restate Theorem 3 from scratch.
3. For Proposition 6, prove the source/model row identities for
   `subSubDiversity`, `subFullDiversityFallback`, `fullSubDiversity`, and
   `fullFullDiversityFallback`, then use the `J2` fallback endpoint above.
4. Keep Proposition 2's corrected-source standardization as a separate seam:
   the lower-tail/Owen/kappa route is already proved, and only the
   source-model applicant-pool/tau substitution remains.

## Do Not Reopen

- Standard Gaussian CDF derivatives, quantile, hazard/Mills, mixture-tail
  cutoff existence, affine upper/lower-tail derivative, and Owen lower-tail
  identities.
- Appendix D.6 formulas from `source.txt` when they disagree with
  `source_tex/proofs.tex`.
- `FINAL_VALIDATION_REPORT.md` polishing until a named result closes or the
  next pause/finalization checkpoint.
