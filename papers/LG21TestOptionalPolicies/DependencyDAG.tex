\documentclass[tikz,border=10pt]{standalone}
\input{../../docs/tikz/dag_preamble.tex}

\begin{document}
\begin{tikzpicture}[
  x=1cm,
  y=1cm,
  every node/.append style={outer sep=4pt, font=\sffamily\small}
]

\dagPaperMetadata{Test-optional Policies: Overcoming Strategic Behavior and Informational Gaps}{Zhi Liu and Nikhil Garg}{EAAMO 2021 version}{2021}{https://arxiv.org/pdf/2107.08922}
\dagPaperLegendRightOfMetadata{
  \node[dag_model, dag_template_legend] (legModel) at (0,0) {formalized\\definition};
  \node[dag_lemma, dag_template_legend] (legLemma) at (4.2,0) {formalized\\lemma};
  \node[dag_result, dag_template_legend] (legResult) at (8.4,0) {formalized\\result};
}
\daglegend{(legModel)(legLemma)(legResult)}{Legend}

\begin{dagPaperBody}

\node[dag_lemma, text width=4.6cm] (AuxFinite) at (0,0) {
  \textbf{Finite admissions support}\\
  base/test mass, selection, and conditional-expectation identities used by the source model
};

\node[dag_lemma, text width=4.6cm] (SharedProb) at (6.1,0) {
  \textbf{Shared testing probability library}\\
  Gaussian posterior laws, lower-tail means, cutoff deviations, and resampling kernels
};

\node[dag_model, text width=4.6cm] (Model) at (12.2,0) {
  \textbf{Source model (Secs. 2--4)}\\
  access status $Z$, information sets, requirement policies, $(Y,X)$ actions, and PMF/law policy surfaces
};

\node[dag_model, text width=4.0cm] (Def1) at (18.3,0) {
  \textbf{Definition 1 (Sec. 2)}\\
  source action space, decisions, feasibility, best response, consistency
};

\node[dag_result, text width=4.6cm] (Thm31) at (24.4,0) {
  \textbf{Theorem 3.1 (Sec. 3)}\\
  hidden access forces strategic withholding under optional-reporting and report-required policies
};

\node[dag_result, text width=4.6cm] (Thm32) at (30.5,0) {
  \textbf{Theorem 3.2 (Sec. 3)}\\
  hidden-access fairness implies test blankness/no test relevance in both policy regimes
};

\node[dag_model, text width=4.0cm] (FairDefs) at (12.2,-4.8) {
  \textbf{Definitions 2--5 (Sec. 2)}\\
  latent-skill fairness, observable fairness, demographic fairness, and test-blankness
};

\node[dag_lemma, text width=4.6cm] (Lemma41) at (18.3,-4.8) {
  \textbf{Lemma 4.1 (Sec. 4)}\\
  fully specified observed-access source equilibria force $(Y,X)=(1,1)$
};

\node[dag_result, text width=4.6cm] (Prop42) at (24.4,-4.8) {
  \textbf{Proposition 4.2 (Sec. 4.1)}\\
  Bayesian optimal estimation with observed access is not latent-skill fair
};

\node[dag_result, text width=4.6cm] (Prop43) at (30.5,-4.8) {
  \textbf{Proposition 4.3 (Sec. 4.1)}\\
  full Bayesian optimal estimation with observed access is not observable or demographic fair
};

\node[dag_model, text width=4.0cm] (Def6) at (12.2,-9.7) {
  \textbf{Definition 6 (Sec. 4.2)}\\
  conditional signal resampling plus common estimate map
};

\node[dag_result, text width=4.0cm] (Thm44) at (18.3,-9.7) {
  \textbf{Theorem 4.4 (Sec. 4.2)}\\
  the resampling policy is strategy-proof, observable fair, and demographic fair
};

% Edges record the formal proof dependencies between source-facing results.
\draw[dag_arrow] (AuxFinite.east) -- (SharedProb.west);
\draw[dag_arrow] (SharedProb.east) -- (Model.west);
\draw[dag_arrow] (SharedProb.south) |- (Def6.west);
\draw[dag_arrow] (Model.east) -- (Def1.west);
\draw[dag_arrow] (Model.south) -- (FairDefs.north);
\draw[dag_arrow] (Def1.east) -- (Thm31.west);
\draw[dag_arrow] (Thm31.east) -- (Thm32.west);
\draw[dag_arrow] (FairDefs.east) -- (Lemma41.west);
\draw[dag_arrow] (Lemma41.east) -- (Prop42.west);
\draw[dag_arrow] (Prop42.east) -- (Prop43.west);
\draw[dag_arrow] (Def6.east) -- (Thm44.west);
\draw[dag_arrow] (Lemma41.south) -- (Thm44.north);

\end{dagPaperBody}
\end{tikzpicture}
\end{document}
