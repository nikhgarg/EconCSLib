\documentclass[tikz,border=10pt]{standalone}
\input{../../docs/tikz/dag_preamble.tex}

\begin{document}
\begin{tikzpicture}[
  x=1cm,
  y=1cm,
  every node/.append style={outer sep=4pt, font=\sffamily\small}
]

\dagPaperMetadata{Test-optional Policies: Overcoming Strategic Behavior and Informational Gaps}{Zhi Liu and Nikhil Garg}{EAAMO 2021 version}{2021}{https://arxiv.org/pdf/2107.08922}
\dagPaperLegendRightOfMetadata{
  \node[dag_result, dag_template_legend] (legAux) {formalized\\auxiliary};
  \node[dag_partial, right=0.45cm of legAux, dag_template_legend] (legPartial) {partially\\formalized};
  \node[dag_caveat_legend, right=0.45cm of legPartial] (legCav) {formalized\\with caveat};
  \node[dag_conditional, right=0.45cm of legCav, dag_template_legend] (legCond) {conditional};
  \node[dag_unformalized, right=0.45cm of legCond, dag_template_legend] (legOpen) {not started};
}
\daglegend{(legAux)(legPartial)(legCav)(legCond)(legOpen)}{Legend}

\begin{dagPaperBody}

\node[dag_result, text width=4.6cm] (AuxFinite) at (0,0) {
  \textbf{Finite admissions support}\\
  Base/test mass, selection, conditional-expectation decompositions
};

\node[dag_partial, text width=4.8cm] (SharedProb) at (5.9,0) {
  \textbf{Shared testing probability library}\\
  posterior wrappers, Gaussian law comparison, cutoff deviations, and conditional resampling
};

\node[dag_partial, text width=4.8cm] (Model) at (11.8,0) {
  \textbf{Source model (Secs. 2--4)}\\
  finite kernels, $(Y,X)$ action object, and PMF/law policy surfaces; concrete Gaussian payoff wiring remains open
};

\node[dag_caveat, text width=4.0cm] (Def1) at (17.7,0) {
  \textbf{Definition 1 (Sec. 2)}\\
  concrete access action space and abstract equilibrium predicate encoded
};

\node[dag_partial, text width=4.8cm] (Thm31) at (23.6,0) {
  \textbf{Theorem 3.1 (Sec. 3)}\\
  reporting-threshold support proved; strategic withholding endpoint remains conditional
};

\node[dag_conditional, text width=4.8cm] (Thm32) at (29.5,0) {
  \textbf{Theorem 3.2 (Sec. 3)}\\
  PMF/law endpoint and contrapositive wrappers; source implication remains conditional
};

\node[dag_caveat, text width=4.0cm] (FairDefs) at (11.8,-4.2) {
  \textbf{Definitions 2--5 (Sec. 2)}\\
  PMF/law fairness and test-blank predicates; mixture implication chain proved
};

\node[dag_partial, text width=4.8cm] (Lemma41) at (17.7,-4.2) {
  \textbf{Lemma 4.1 (Sec. 4)}\\
  optional-reporting and report-required no-deviation contradictions proved
};

\node[dag_partial, text width=4.8cm] (Prop42) at (23.6,-4.2) {
  \textbf{Proposition 4.2 (Sec. 4.1)}\\
  four-group, fixed-base posterior law, and source-shaped Gaussian-law cores proved
};

\node[dag_partial, text width=4.8cm] (Prop43) at (29.5,-4.2) {
  \textbf{Proposition 4.3 (Sec. 4.1)}\\
  witness, posterior-precision, fixed-base posterior, and point-vs-Gaussian cores proved
};

\node[dag_caveat, text width=4.0cm] (Def6) at (11.8,-8.6) {
  \textbf{Definition 6 (Sec. 4.2)}\\
  finite conditional-kernel re-sampling policy
};

\node[dag_caveat, text width=4.0cm] (Thm44) at (17.7,-8.6) {
  \textbf{Theorem 4.4 (Sec. 4.2)}\\
  finite distributional core of observable and demographic fairness
};

% Edges: solid only for verified auxiliary support; dashed for source roadmap debt.
\draw[dag_arrow] (AuxFinite.east) -- (SharedProb.west);
\draw[dag_dashed_arrow] (SharedProb.east) -- (Model.west);
\draw[dag_arrow] (SharedProb.south) |- (Def6.west);
\draw[dag_dashed_arrow] (Model.east) -- (Def1.west);
\draw[dag_dashed_arrow] (Model.south) |- (FairDefs.west);
\draw[dag_dashed_arrow] (Def1.east) -- (Thm31.west);
\draw[dag_dashed_arrow] (Thm31.east) -- (Thm32.west);
\draw[dag_dashed_arrow] (FairDefs.east) -- (Lemma41.west);
\draw[dag_dashed_arrow] (Lemma41.east) -- (Prop42.west);
\draw[dag_dashed_arrow] (Prop42.east) -- (Prop43.west);
\draw[dag_arrow] (Def6.east) -- (Thm44.west);
\draw[dag_dashed_arrow] (Lemma41.south) -- (Thm44.north);

\end{dagPaperBody}
\end{tikzpicture}
\end{document}
